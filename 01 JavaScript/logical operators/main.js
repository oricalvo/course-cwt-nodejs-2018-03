// function run(str) {
//     //str = str || "abc";
//     str = str && "abc";
//
//     console.log(str);
// }
//
// run(false);

var res = root && root.x && root.x.y;

var res = gc && gc.collect() && gc.collect().y;
